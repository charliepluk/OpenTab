/** @jsx React.DOM */
var React = require('react/addons');

require('./css')('\
');

var Slider = React.createClass({displayName: 'Slider',
  render: function() {
    var min = this.props.min;
    var max = this.props.max;
    var value = this.props.value;

    return (
      React.DOM.div(null, "SLIDER")
    );
  }
});

module.exports = Slider;
